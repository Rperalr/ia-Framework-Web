<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.nttdata.enterprise.automation</groupId>
    <artifactId>automation-ai</artifactId>
    <version>1.0.0</version>
    <packaging>jar</packaging>

    <name>Enterprise Automation AI</name>
    <description>AI agent orchestration, MCP abstractions and Jira/GitHub integration library.</description>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <maven.compiler.release>11</maven.compiler.release>
        <automation.core.version>1.0.0</automation.core.version>
        <slf4j.version>1.7.36</slf4j.version>
    </properties>

    <dependencies>
        <dependency>
            <groupId>com.nttdata.enterprise.automation</groupId>
            <artifactId>automation-core</artifactId>
            <version>${automation.core.version}</version>
        </dependency>
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-api</artifactId>
            <version>${slf4j.version}</version>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.13.0</version>
                <configuration><release>${maven.compiler.release}</release></configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-source-plugin</artifactId>
                <version>3.3.1</version>
                <executions>
                    <execution><id>attach-sources</id><goals><goal>jar-no-fork</goal></goals></execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jar-plugin</artifactId>
                <version>3.4.2</version>
                <configuration>
                    <archive>
                        <manifest>
                            <mainClass>com.nttdata.enterprise.orchestration.AutomationOrchestrator</mainClass>
                        </manifest>
                    </archive>
                </configuration>
            </plugin>
        </plugins>
    </build>

    <distributionManagement>
        <repository>
            <id>enterprise-maven-releases</id>
            <name>Enterprise Maven Releases</name>
            <url>${env.MAVEN_RELEASES_URL}</url>
        </repository>
        <snapshotRepository>
            <id>enterprise-maven-snapshots</id>
            <name>Enterprise Maven Snapshots</name>
            <url>${env.MAVEN_SNAPSHOTS_URL}</url>
        </snapshotRepository>
    </distributionManagement>

</project>
